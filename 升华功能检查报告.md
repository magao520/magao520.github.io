# 废土交易所 v12.0 升华功能检查报告

## 检查范围
- `/workspace/farm-game/js/game.js` (2305行)
- `/workspace/farm-game/index.html` (413行)

---

## 1. 角色等级经验系统

**判断: 已实现**

- `addExp(amount)` (第277行): 完整实现，包含经验累加、升级检测、升级提示、调用 `levelUpStats()` 和 `updateLevelPanel()`、`save()`。
- `levelUpStats()` (第284行): 升级时随机增加1-3点属性点（luck/charm/agility），上限100。
- `updateLevelPanel()` (第288行): 更新 `#level-panel` DOM元素，显示等级、称号、经验条。
- **胜利时获得经验**: `showModal()` (第774行) 中 `const expGain=w?(10+G.level*2):5; addExp(expGain);` -- 胜利获得更多经验，失败也获得少量经验。
- `load()` (第266行): 从 localStorage 恢复 level/exp/stats。
- `save()` (第267行): 持久化保存。
- `initApp()` (第1702行): 初始化时调用 `updateLevelPanel()`。
- index.html 第282行: `#level-panel` 元素存在。

**无问题。**

---

## 2. 角色属性系统

**判断: 已实现**

- `G.stats = {luck:10, charm:10, agility:10}` (第35行): 初始化。
- **幸运影响游戏**:
  - 骰子围骰重投 (第1464行): `if(isTrips&&G.stats.luck>30&&Math.random()<0.15)` -- 幸运>30时15%概率重投围骰。
  - 炸金花看牌感应 (第1511行): `if(G.stats.luck>50&&Math.random()<0.05)` -- 幸运>50时5%概率看到对手一张牌。
- **魅力影响游戏**:
  - 炸金花加注上限 (第1530行): `const maxBet=50+G.stats.charm*2` -- 魅力越高，加注上限越高。
- **敏捷影响游戏**:
  - 移动速度 (第1908行): `speed*=1+G.stats.agility/200` -- 敏捷越高，移动越快。
- 设置面板显示属性 (第190行): `settings-stats` 元素。
- 升级自动加点 (第284-287行): `levelUpStats()`。
- `load()/save()` 持久化。

**无问题。三个属性均实际影响游戏机制。**

---

## 3. 角色技能树

**判断: 已实现**

- 6个角色各有2个技能，定义在 `CHARACTERS` 数组 (第240-245行)。
- `refreshSkills()` (第1297行): `ch.skills[0].unlocked=G.level>=5; ch.skills[1].unlocked=G.level>=10` -- 5级解锁第一个技能，10级解锁第二个。
- **技能实际生效**:
  - `peek10` (信使直觉): 炸金花看牌时10%概率看到对手一张牌 (第1507-1508行)。
  - `farChat` (顺风耳): 聊天距离从300扩展到600 (第997-998行)。
  - `nightSpeed` (夜行): 夜晚移动速度+20% (第1911行)。
  - `diceBonus` (敏锐): 骰子猜对时5%额外胜率 (第1490-1491行)。
  - `teamReward` (忠诚): 队友获胜时10%奖励 (第776行)。
  - `findScrap` (嗅觉): 初始废金属数量从15增加到20 (第1747行)。
  - `bluff15` (狡黠): 诈唬成功率从50%提升到65% (第1515-1516行)。
  - `taxCut` (商人): 交易税从5%降为0% (第881-883行)。
  - `hoard20` (囤积): 初始物资从50增加到70 (第1354-1355行)。
  - `scrapValue` (挖掘): 废金属价值翻倍从1到2 (第1965行)。
  - `climb` (攀爬): 边界margin从16变为0 (第1918行)。
  - `regen5` (再生): 每局恢复5物资 (第775行)。
- 设置面板显示技能状态 (第192行)。

**无问题。所有12个技能均有实际游戏效果。**

---

## 4. 角色皮肤

**判断: 已实现**

- 每个角色有3个皮肤，定义在 `CHARACTERS[].skins` (第240-245行)。
- `renderSkinChoices()` (第206行): 渲染皮肤选择UI。
- `selectSkin()` (第216行): 选择皮肤，检查解锁条件（默认/Lv5/Lv15），更新 `G.skinIndex`。
- 皮肤影响显示: 全局多处使用 `ch.skins[G.skinIndex||0]` 获取当前emoji，包括 `selectCharModal`、`publishPresence`、`sendChat`、`sendEmote`、`showModal`、`Lobby.init()` 等。
- `load()/save()`: 持久化 `G.skinIndex`。

**无问题。**

---

## 5. 昼夜循环

**判断: 已实现**

- `dayNightCycle` 在 `update()` 中递增 (第1897行): `this.dayNightCycle+=dt`。
- `gameTime` 计算 (第1898行): `this.gameTime=(this.dayNightCycle/24)%24` -- 1秒=1游戏分钟，24分钟=1游戏日。
- `dayNightAlpha` 在 `update()` 中根据小时变化 (第1900-1902行):
  - 6-18时: alpha=0 (白天)
  - 18-20时: alpha渐变到0.3 (黄昏)
  - 20-6时: alpha=0.5 (夜晚)
- `_drawClock()` (第2287行): 在 `draw()` 末尾调用 (第2078行)，显示游戏时间钟表，带日夜指示灯。
- 夜晚遮罩绘制 (第2066行): `if(this.dayNightAlpha>0.01){ctx.fillStyle='rgba(10,10,30,...)';ctx.fillRect(...)}`
- 夜晚灯笼光晕 (第2072行): `if(this.dayNightAlpha>0.2)` 为每张桌子绘制暖色光晕。
- 夜晚影响移动速度 (第1911行): 夜行技能生效。
- 夜晚NPC危险标记 (第988行): `isDangerous=isNight&&Math.random()<0.3`。

**无问题。完整的昼夜循环系统。**

---

## 6. 动态天气

**判断: 已实现**

- `initWeather()` (第1813行): 初始化天气，设置首次变化时间。
- `updateWeather(dt)` (第1820行): 在 `update()` 中调用 (第1922行)。
  - 定时切换天气类型: clear/sandstorm/rad rain/fog。
  - 生成天气粒子: 辐射雨绿色雨滴、沙尘暴沙粒、浓雾大颗粒。
  - 辐射雨收集器: 每30秒获得1-3单位物资 (第1838-1842行)。
  - 沙尘暴减速: `speed*=0.8` (第1909行)。
  - 天气同步: `broadcastWeather()` 通过MQTT同步给其他玩家。
- 天气粒子绘制 (第2048-2052行): 在 `draw()` 中遍历 `weatherParticles` 绘制。
- 天气遮罩绘制 (第2068-2070行): 沙尘暴/辐射雨/浓雾各有不同颜色遮罩。
- `_drawWeatherUI()` (第2081行): 在 `draw()` 中调用 (第2076行)，左上角显示天气图标和名称。
- 环境音效联动: `Sound.startAmbient(G.weather.type)` 在天气变化时调用 (第1833行)。
- 浓雾影响NPC/玩家可见度 (第1781行, 第2031行): 距离>300时alpha降低。

**无问题。完整的动态天气系统。**

---

## 7. 可交互环境

**判断: 已实现**

- **物资箱 (lootCrates)**:
  - 初始化 (第1744行): 5个物资箱分布在地图各处。
  - 绘制 (第2015行): `for(const crate of this.lootCrates){this._drawCrate(ctx,crate)}`。
  - 交互 (第1952行, 第1962行): 靠近时提示"按E打开"，按E调用 `openCrate()`，获得物资或废金属，60秒冷却。
  - `_drawCrate()` (第2126行): 绘制箱子外观、开启动画、冷却计时。
- **油桶 (barrels)**:
  - 初始化 (第1745行): 5个油桶。
  - 绘制 (第2017行): `for(const b of this.barrels){...this._drawBarrel(ctx,b)}`。
  - 交互 (第1954行, 第1969行): 靠近时提示"按E踢油桶"，踢后物理滚动，停下后爆炸，产生屏幕震动和粒子效果。
  - `_drawBarrel()` (第2136行): 绘制油桶和爆炸动画。
  - 爆炸影响附近NPC (第1977行): NPC显示"啊！"气泡。
  - 30秒后重生 (第1939行)。
- **传送门 (portal)**:
  - 初始化 (第1746行): 位于(100,750)。
  - 绘制 (第2019行): `if(this.portal)this._drawPortal(ctx,this.portal)`。
  - 交互 (第1941行): 靠近时自动传送到地图随机端点。
  - `_drawPortal()` (第2143行): 绘制脉动光环和传送粒子。
- **长椅 (benches)**:
  - 初始化 (第1721行): 3个长椅。
  - 绘制 (第2021行): 绘制长椅外观。
  - 交互 (第1956行, 第817行): 靠近时提示"按E坐下"，坐后emoji变为冥想姿势，按Z站起。

**无问题。所有可交互环境元素均已实现。**

---

## 8. NPC系统

**判断: 已实现**

- `initNPCs()` (第1758行): 4个NPC -- 老杰克(商人)、流浪者(乞丐)、守卫(巡逻)、拾荒者(商人)。
- `updateNPCs(dt)` (第1766行): 在 `update()` 中调用 (第1922行)。
  - 状态机: wander/idle/interact 三种状态切换。
  - 对话气泡: 随机间隔显示对话文字，4秒后消失。
  - 移动AI: wander状态时向随机目标点移动。
  - 守卫巡逻: 在巡逻中心点附近巡逻 (第1776行)。
  - 靠近检测: 距离<40时进入interact状态。
- `drawNPCs(ctx)` (第1779行): 在 `draw()` 中调用 (第2027行)。
  - 绘制NPC emoji、名字标签、对话气泡。
  - 浓雾天气下远处NPC透明度降低。
- `checkNPCInteraction()` (第1789行): 在 `checkTableInteraction()` 中调用 (第1957行)。
  - 商人NPC: 提示"按E交易"，打开 `showShopPanel()`。
  - 乞丐NPC: 提示"按E给物资"，给5单位获得临时幸运+10。
  - 守卫NPC: 提示"守卫正在巡逻"。

**无问题。完整的NPC系统。**

---

## 9. 地图区域

**判断: 已实现**

- 三区域定义 (第2096-2101行):
  - `slum` (贫民区): x:0-800, y:800-1500 -- 颜色 `#3a3020`
  - `black` (黑市区): x:1200-2000, y:0-700 -- 颜色 `#4a2020`
  - `safe` (安全区): x:600-1400, y:400-1100 -- 颜色 `#4a4028`
- `_drawFloor()` (第2104行): 根据区域使用不同地板颜色，棋盘格交替明暗。
- 区域切换检测 (第1923-1924行): `update()` 中检测区域变化，触发闪烁效果和toast提示。
- 区域闪烁效果 (第2074行): `draw()` 中绘制区域切换闪烁。
- 小地图区域底色 (第2274-2276行): 小地图上用不同颜色标识三个区域。
- 区域名称: `getRegionName()` 返回"贫民区"/"黑市区"/"安全区"。
- 黑市区桌子特殊样式 (第2236行, 第2240行): 红色光晕和边框。
- 黑市区基础赌注翻倍 (第1358行): `const regionMult=table&&table.region==='black'?2:1`。

**无问题。三区域地板颜色不同，区域提示通过toast和闪烁效果显示。**

---

## 10. 炸金花升级 -- 梭哈/诈唬按钮

**判断: 已实现**

- `renderZJH()` (第1397行):
  - 诈唬按钮 (第1408行): `const bluffBtn=!me.seen&&!me.bluffed?'<button ... onclick="doAction('bluff')">诈唬</button>':''` -- 未看牌且未诈唬过时显示。
  - 梭哈按钮 (第1409行): `const allInBtn='<button ... onclick="doAction('allin')">梭哈</button>'` -- 始终显示。
  - 按钮在 `G.myTurn&&!G.gameOver` 时渲染到操作栏 (第1410行)。
- `doZJHAction()` 中:
  - `bluff` (第1513行): 诈唬逻辑，成功时下次跟注半价。
  - `allin` (第1519行): 梭哈逻辑，押上全部筹码。
- `handleRemoteAction()` (第1625行): 处理远程bluff/allin消息。

**无问题。**

---

## 11. 21点分牌双倍 -- 分牌/双倍/保险按钮

**判断: 已实现**

- `renderBJ()` (第1414行):
  - 分牌按钮 (第1432, 1435行): `canSplit=!me.hands&&me.cards.length===2&&me.cards[0].r===me.cards[1].r&&me.chips>=me.bet` -- 两张同点且筹码足够时显示。
  - 双倍按钮 (第1433, 1436行): `canDouble=!me.hands&&me.cards.length===2&&me.chips>=me.bet` -- 仅两张牌且筹码足够时显示。
  - 保险按钮 (第1434, 1437行): `canInsure=!me.insured&&others.length>0&&others[0].cards[0].r==='A'` -- 对手首牌为A时显示。
  - 按钮在 `G.myTurn&&!G.gameOver` 时渲染 (第1438行)。
- `doBJAction()` 中:
  - `split` (第1569行): 分牌逻辑，创建两手牌。
  - `double` (第1577行): 双倍下注逻辑，只能再要一张。
  - `insure` (第1585行): 保险逻辑，花费半注购买。
- 序列化/反序列化支持 hands/currentHand/doubled/insured 字段。

**无问题。**

---

## 12. 骰子围骰奖池

**判断: 已实现**

- `G.diceJackpot` (第39行): 奖池初始化为0。
- `G.diceJackpotStreak` (第39行): 连续无围骰计数。
- 每局开始时扣1单位进入奖池 (第1360行): `const jackpotFee=players.length; G.diceJackpot+=jackpotFee`。
- `renderDice()` (第1466行):
  - 奖池显示 (第1471行): `const jpEl=document.getElementById('jackpot-hint')` -- 创建或更新奖池提示元素，显示 `围骰奖池: X单位 (连Y局无围骰)`。
- `checkDiceEnd()` (第1476行):
  - 围骰时 (第1478-1483行): 奖池由围骰玩家平分。
  - 连续4局无围骰时 (第1486-1488行): 奖池由所有玩家平分。
- 序列化/反序列化支持 diceJackpot/diceJackpotStreak。

**无问题。**

---

## 13. 观战系统

**判断: 已实现**

- `spectateTableByCode(code)` (第1309行): 函数存在，发送 `spectate` 消息加入观战。
- `G.isSpectator` (第41行): 观战状态标记。
- `G.spectators` (第40行): 观战者列表。
- 服务端处理 (第1173-1188行):
  - `spectate`: 主机添加观战者，发送 `spectate-welcome`。
  - `spectate-full`: 观战席已满提示（最多2人）。
  - `spectate-welcome`: 观战者接收游戏状态，可看到所有牌面。
  - `spectate-leave`: 观战者离开。
  - `spectate-chat`: 观战者聊天。
- `sendSpectatorChat()` (第1314行): 观战者发送聊天消息。
- `leaveSpectate()` (第1323行): 离开观战。
- 观战模式UI:
  - `renderZJH/renderBJ/renderDice` 中 `isSpec` 判断 (第1400, 1427, 1472行): 观战者可以看到所有牌面。
  - 观战提示 (第1404, 1427行): 显示"[观战模式]"。
  - 操作栏显示"退出观战"按钮 (第1406, 1429, 1472行)。

**无问题。**

---

## 14. 牌局回放

**判断: 已实现**

- `loadReplays()/saveReplays()/addReplay()` (第510-512行): 保存最近10条牌局记录。
- `saveReplayFromGame(won)` (第782行): 在 `showModal()` 中调用 (第780行)，解析游戏日志提取关键操作。
- `showStatsPanel()` (第513行):
  - 加载最近5条回放 (第517行): `const replays=loadReplays().slice(-5).reverse()`。
  - 渲染回放列表 (第519-528行): 显示日期、游戏类型、获胜者、关键操作。

**无问题。**

---

## 15. 好友系统

**判断: 已实现**

- `showFriendPanel()` (第617行): 函数存在，创建好友列表面板。
- `renderFriendPanelList()` (第623行): 渲染在线/离线好友列表，在线好友显示"私聊"和"邀请"按钮。
- `showFriendMenu()` (第569行): 点击好友弹出操作菜单（删除好友/发送私聊/邀请组队/查看资料）。
- `addFriend()/removeFriend()/updateFriendStatus()` (第541-553行): 增删改查。
- `renderFriends()` (第554行): 渲染收藏列表（在线列表中点击玩家添加好友）。
- 在线列表点击添加好友 (第1046行): `onclick="addFavorite(...)"` -- `addFavorite` 调用 `addFriend`。
- MQTT私聊 (第596-616行): `TOPIC_WHISPER` 支持私聊、组队邀请、拍卖通知。
- index.html 第265行: "好友"按钮调用 `showFriendPanel()`。
- `updateFriendCountUI()` (第564行): 更新好友数量显示。

**无问题。**

---

## 16. 公会/阵营系统

**判断: 已实现**

- `TOPIC_GUILD_CHAT` (第638行): 阵营聊天MQTT主题。
- `createGuild()/joinGuildByCode()/leaveGuild()` (第643-663行): 创建/加入/退出阵营。
- `donateToGuild()/distributeFromGuild()` (第664-676行): 捐献/分配物资。
- `getGuildBuff()` (第682行): 阵营buff -- 同阵营2人以上在房间时幸运+5。
- `publishGuildPresence()/handleGuildChatMsg()/sendGuildChat()` (第688-705行): 阵营聊天。
- `updateGuildUI()` (第706行): 更新设置面板中的阵营信息。
- `showGuildPanel()` (第712行): 阵营面板UI，支持创建/加入/捐献/聊天/退出。
- `getGuildTag()` (第642行): 名字标签前显示阵营标签。
- `_drawNameTag()` (第2200行): 使用 `getGuildTag()` 在名字前添加阵营标签。
- index.html 第365行: 设置面板中有"阵营"行，显示 `settings-guild`。
- MQTT订阅阵营聊天 (第1100行): `G.mqtt.subscribe(TOPIC_GUILD_CHAT/${g.tag})`。

**无问题。**

---

## 17. 交易系统

**判断: 已实现**

- `TOPIC_TRADE` (第879行): 交易MQTT主题。
- `requestTrade()/acceptTrade()` (第885-900行): 发起/接受交易请求。
- `openTradePanel()` (第901行): 打开交易面板UI，显示双方信息和出价。
- `confirmTrade()/finalizeTrade()/cancelTrade()` (第917-952行): 确认/完成/取消交易。
- `handleTradeMsg()` (第953行): 处理MQTT交易消息。
- `getTradeTaxRate()` (第880行): 交易税率，受商人技能 `taxCut` 影响。
- MQTT订阅 (第1100行): `G.mqtt.subscribe(TOPIC_TRADE/+/+/${G.myId})`。
- 在线列表中点击玩家可触发交易（通过好友菜单的"发送私聊"间接触发，或直接通过 `requestTrade`）。

**注意**: 在线列表的 `addFavorite` 只添加好友，没有直接的"交易"按钮。交易需要先通过好友菜单或私聊发起。这不算bug，但交易入口不够直观。

**基本已实现，交易入口略显隐蔽。**

---

## 18. 邮件系统

**判断: 已实现**

- `showMailPanel()` (第441行): 函数存在，创建邮件列表面板。
- `loadMail()/saveMail()/addMail()` (第426-434行): 增删改查，最多30封。
- `showMailDetail()` (第457行): 查看邮件详情。
- `claimMailAttachment()` (第471行): 领取邮件附件（chips/can/gas/match）。
- `deleteMail()` (第481行): 删除邮件。
- `updateMailBadge()` (第436行): 更新未读数量角标。
- `cleanOldMail()` (第484行): 清理超量邮件。
- 邮件触发场景:
  - 每日救济金 (第325行)
  - 拍卖过期退回 (第341行)
  - 拍卖成交通知 (第375行)
  - 交易完成通知 (第940行)
  - 阵营分配通知 (第674行)
- index.html 第266行: "邮件"按钮带未读角标 `#mail-badge`。

**无问题。**

---

## 19. 多币种

**判断: 已实现**

- `G.currency = {can:0, gas:0, match:0}` (第42行): 三种专用货币。
- `loadCurrency()/saveCurrency()` (第1632-1633行): 持久化。
- 胜利奖励 (第777行): 炸金花胜利+1罐头，21点胜利+1汽油，骰子胜利+1火柴。
- `convertCurrency()` (第1646行): 货币兑换功能。
- `showCurrencyPanel()` (第1660行): 兑换面板UI。
- 拍卖行支持多币种 (第349-353行, 第365-368行): 挂售和购买支持chips/can/gas/match。
- index.html: 设置面板中有"货币"行，点击"兑换"按钮打开兑换面板 (第366行)。
- **UI显示问题**: 顶栏只显示通用单位 (`#user-chips`)，没有直接显示 can/gas/match 的数量。需要进入设置面板才能看到。

**已实现，但多币种数量未在主界面直接显示，只能通过设置->兑换面板查看。**

---

## 20. NPC商店

**判断: 已实现**

- `showShopPanel()` (第1680行): 函数存在，创建商店面板UI。
- 商品列表:
  - 幸运药水 (15单位) -- 下一局幸运+20
  - 透视眼镜 (30单位) -- 炸金花看对手一张暗牌
  - 加速药剂 (10单位) -- 移动速度+50%
  - 护盾 (20单位) -- 输时只损失一半
  - 改名卡 (50单位) -- 修改角色名称
- `buyShopItem()` (第1685行): 购买逻辑，扣除物资，物品进入背包。
- `useInventoryItem()` (第1637行): 使用物品逻辑。
- `showInventoryPanel()` (第1665行): 背包面板。
- NPC商人交互触发 (第1801行): `openNPCShop(npc)` 调用 `showShopPanel()`。
- index.html 第369行: 设置面板中有"商店"按钮。

**无问题。**

---

## 21. 拍卖行

**判断: 已实现**

- `renderAuctionPanel()` (第381行): 函数存在，创建拍卖行面板UI。
- `loadAuction()/saveAuction()` (第333-334行): 持久化，最多20条。
- `listAuction(item)` (第343行): 挂售物品，支持chips/can/gas/match和背包物品。
- `buyAuction(id)` (第356行): 购买拍卖物品，含10%税。
- `cleanupExpiredAuctions()` (第335行): 清理过期拍卖，退回物品并发送邮件通知。
- `showListAuctionModal()` (第407行): 挂售物品选择面板。
- `submitListAuction()` (第412行): 提交挂售。
- MQTT通知 (第376-378行): 拍卖售出时通过私聊通知卖家。
- index.html 第368行: 设置面板中有"拍卖行"按钮。
- `initApp()` (第1702行): 初始化时调用 `cleanupExpiredAuctions()`。

**无问题。**

---

## 22. 角色行走动画

**判断: 已实现**

- `_drawAnimatedPlayer()` (第2207行): 完整的动画绘制函数。
  - 行走状态: `offsetY=Math.sin(time*Math.PI*4)*3` -- 上下弹跳。
  - 待机状态: `scaleY=0.98+Math.sin(time*Math.PI*1.5)*0.02` -- 呼吸缩放。
  - 坐下状态: 不弹跳。
  - 表情反应: `bounce=1+Math.sin(t*Math.PI*3)*0.15*(1-t)` -- 弹跳衰减。
  - 朝向: `scaleX=faceDir` -- 根据faceDir水平翻转。
- 在 `draw()` 中调用:
  - 自己 (第2039行): `_drawAnimatedPlayer(ctx,this.me.x,this.me.y,this.me.emoji,this.me,true,this.time)`
  - 其他玩家 (第2034行): `_drawAnimatedPlayer(ctx,p.x,p.y,p.emoji||'...',p,false,this.time)`
- `animState` 在 `update()` 中更新 (第1914-1917行): walk/idle根据移动状态切换。

**无问题。**

---

## 23. 牌桌动态效果 -- CSS动画

**判断: 已实现**

index.html 中定义的CSS动画:
- `@keyframes flicker` (第72行): 蜡烛闪烁动画 -- `candle-glow` 元素。
- `@keyframes cardFlip` (第164行): 翻牌动画 -- `card-flip` 类。
- `@keyframes cardDeal` (第166行): 发牌动画 -- `card-deal` 类。
- `@keyframes diceRollAnim` (第168行): 骰子滚动动画 -- `dice-roll` 类。
- `@keyframes glowPulse` (第156行): 玩家名光晕脉动 -- `glow-pulse` 类。
- `@keyframes pulse` (第122行): 等待按钮脉动。
- `@keyframes shake` (第231行): 屏幕震动 -- `shake` 类。
- `@keyframes dealIn` (第229行): 发牌入场动画。
- `@keyframes toastIn/toastOut` (第201行): toast弹出/消失动画。
- `@keyframes fadeIn` (第214行): 淡入动画。
- `@keyframes spin` (第226行): 加载旋转动画。

**无问题。丰富的CSS动画效果。**

---

## 24. 环境音效 -- Sound对象扩展

**判断: 已实现**

Sound对象 (第53行) 扩展了以下功能:
- **环境音效 `startAmbient(weatherType)`** (第81行):
  - `clear`: 低通滤波白噪音（风声）。
  - `sandstorm`: 高通滤波白噪音 + LFO调制（沙尘声）。
  - `rad rain`: 低通滤波白噪音 + 随机雨滴脉冲。
  - `fog`: 低频正弦波 + LFO（低沉嗡鸣）。
- `stopAmbient()` (第115行): 停止环境音。
- **牌桌BGM `startRoomBGM(gameType)`** (第123行):
  - `zjh`: 锯齿波低音 + LFO + 随机高频音效（紧张氛围）。
  - `bj`: 摇摆爵士音符序列 + 三角波低音。
  - `dice`: 方波节拍 + 滤波 + 随机打击音效。
- `stopRoomBGM()` (第149行): 停止牌桌BGM。
- **大厅BGM `startBGM()`** (第157行): 正弦波低音 + LFO。
- `stopBGM()` (第171行): 停止大厅BGM。
- 额外音效: `step`(脚步), `join`(加入), `chat`(聊天), `cardSlide`(滑牌), `uiHover`(UI悬停), `chipMetal`(金属筹码), `diceRoll`(骰子滚动)。
- `_ambientGain` (第54行): 独立环境音增益节点。
- `_bgmGain` (第54行): 独立BGM增益节点。
- `_sfxGain` (第55行): 独立音效增益节点。

**无问题。三层音效系统（环境音/牌桌BGM/大厅BGM）完整实现。**

---

## 额外检查项

### 摇杆(joystick)的z-index和位置是否遮挡加入牌桌按钮

**判断: 无遮挡问题**

- 摇杆在Canvas内绘制 (第2115行 `_drawJoystick`)，不是HTML元素，不存在z-index遮挡问题。
- "搭新桌"按钮在 index.html 第284行: `style="position:absolute;top:12px;right:12px;z-index:10"`。
- 摇杆位于Canvas左下角区域（触摸位置），"搭新桌"在右上角，物理位置不重叠。
- 交互提示 `#interact-hint` 在 `bottom:80px` (第278行)，摇杆也在底部区域，但提示是pointer-events:none，不会阻挡触摸。

**无问题。**

### draw()函数中是否遗漏了某些新绘制调用

**判断: 无遗漏**

`draw()` (第2002行) 中的绘制顺序:
1. 清屏 (第2004行)
2. 地板 `_drawFloor` (第2007行)
3. 墙壁 (第2009行)
4. 装饰物 `_cachedDecor` (第2013行)
5. 物资箱 `_drawCrate` (第2015行)
6. 油桶 `_drawBarrel` (第2017行)
7. 传送门 `_drawPortal` (第2019行)
8. 长椅 (第2021行)
9. 牌桌 `_drawTable` (第2023行)
10. 绿色轨迹 (辐射雨) (第2025行)
11. NPC `drawNPCs` (第2027行)
12. 其他玩家 (第2029行)
13. 自己 (第2039行)
14. 行走粒子 (第2042行)
15. 环境粒子 (第2044行)
16. 灰尘粒子 (第2046行)
17. 天气粒子 (第2048行)
18. 小地图 `_drawMinimap` (第2056行)
19. 欢迎消息 `_drawWelcome` (第2058行)
20. 暗角 `_drawVignette` (第2060行)
21. 摇杆 `_drawJoystick` (第2062行)
22. 转场 `_drawTransition` (第2064行)
23. 昼夜遮罩 (第2066行)
24. 天气遮罩 (第2068行)
25. 灯笼光晕 (第2072行)
26. 区域闪烁 (第2074行)
27. 天气UI `_drawWeatherUI` (第2076行)
28. 时钟 `_drawClock` (第2078行)

**所有新增绘制调用均已包含在draw()中。无遗漏。**

### update()函数中是否遗漏了某些新更新调用

**判断: 无遗漏**

`update(dt)` (第1896行) 中的更新顺序:
1. 昼夜循环 `dayNightCycle` (第1897行)
2. 玩家移动和输入处理 (第1903-1918行)
3. 牌桌交互检测 `checkTableInteraction` (第1921行)
4. 光照角度 `_lightAngle` (第1921行)
5. 天气更新 `updateWeather` (第1922行)
6. NPC更新 `updateNPCs` (第1922行)
7. 区域检测 (第1923行)
8. 位置广播 `broadcastPos` (第1925行)
9. 其他玩家更新 (第1926行)
10. 行走粒子 (第1928行)
11. 辐射雨绿色轨迹 (第1929行)
12. 环境粒子 (第1932行)
13. 灰尘粒子 (第1933行)
14. 喷泉粒子 (第1934行)
15. 物资箱更新 (第1935行)
16. 油桶更新 (第1936行)
17. 传送门检测 (第1941行)
18. 废金属收集 (第1942行)
19. 屏幕震动衰减 (第1943行)
20. 转场alpha (第1944行)

**所有新增更新调用均已包含在update()中。无遗漏。**

### index.html中是否有对应的UI元素

**判断: 基本完整，有少量可改进之处**

已存在的UI元素:
- `#level-panel` (第282行): 等级面板
- `#quest-panel` (第280行): 任务面板
- `#interact-hint` (第278行): 交互提示
- `#boundary-warning` (第276行): 边界警告
- `#lobby-canvas` (第272行): 大厅Canvas
- `#mail-badge` (第266行): 邮件角标
- `#chat-bar` (第286行): 聊天栏
- `#settings-modal` 中: 音效/BGM/角色/等级/属性/技能/好友/阵营/货币/背包/拍卖行/商店 (第354-375行)
- `#char-modal` (第377行): 角色选择
- `#create-modal` (第323行): 创建牌桌
- `#result-modal` (第336行): 结算弹窗
- `#rules-modal` (第346行): 规则说明
- `#game-screen` 中: p0/p1/p2 玩家区域、底池、操作栏、蜡烛计时条、游戏日志

**缺失/可改进**:
- 多币种 (can/gas/match) 没有在主界面顶栏直接显示，需要进入设置面板查看。
- 没有专门的"观战"按钮UI（需要通过代码调用 `spectateTableByCode`）。
- 没有专门的"交易"按钮UI（需要通过好友菜单触发）。
- 旧版 `#tables-grid` 和 `#online-panel` 被设为 `display:none` (第300-301行)，功能已迁移到Canvas。

---

## 总结

| # | 功能 | 状态 | 问题 |
|---|------|------|------|
| 1 | 角色等级经验系统 | **已实现** | 无 |
| 2 | 角色属性系统 | **已实现** | 无 |
| 3 | 角色技能树 | **已实现** | 无 |
| 4 | 角色皮肤 | **已实现** | 无 |
| 5 | 昼夜循环 | **已实现** | 无 |
| 6 | 动态天气 | **已实现** | 无 |
| 7 | 可交互环境 | **已实现** | 无 |
| 8 | NPC系统 | **已实现** | 无 |
| 9 | 地图区域 | **已实现** | 无 |
| 10 | 炸金花梭哈/诈唬 | **已实现** | 无 |
| 11 | 21点分牌/双倍/保险 | **已实现** | 无 |
| 12 | 骰子围骰奖池 | **已实现** | 无 |
| 13 | 观战系统 | **已实现** | 无 |
| 14 | 牌局回放 | **已实现** | 无 |
| 15 | 好友系统 | **已实现** | 无 |
| 16 | 公会/阵营系统 | **已实现** | 无 |
| 17 | 交易系统 | **已实现** | 交易入口略显隐蔽，需通过好友菜单发起 |
| 18 | 邮件系统 | **已实现** | 无 |
| 19 | 多币种 | **已实现** | 主界面未直接显示can/gas/match数量 |
| 20 | NPC商店 | **已实现** | 无 |
| 21 | 拍卖行 | **已实现** | 无 |
| 22 | 角色行走动画 | **已实现** | 无 |
| 23 | 牌桌CSS动画 | **已实现** | 无 |
| 24 | 环境音效 | **已实现** | 无 |

**额外检查**:
- 摇杆z-index: 无遮挡问题（Canvas内绘制）
- draw()遗漏: 无遗漏
- update()遗漏: 无遗漏
- index.html UI元素: 基本完整

**结论**: 24个升华功能全部已实现并正确集成到游戏循环中。仅有2个轻微的UI可改进点（多币种主界面显示、交易入口直观性），不影响功能正确性。
